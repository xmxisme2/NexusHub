spring:
  datasource:
    url: jdbc:mysql://${NEXUSHUB_DB_HOST:nexushub-mysql}:${NEXUSHUB_DB_PORT:3306}/nexushub?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: ${NEXUSHUB_DB_USERNAME:root}
    password: ${NEXUSHUB_DB_PASSWORD}
