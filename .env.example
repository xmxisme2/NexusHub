# Docker 本地开发端口；不要提交生产密钥。
# Set a private database password locally; never commit the real value.
NEXUSHUB_DB_PASSWORD=change-me-locally
NEXUSHUB_COOKIE_SECURE=false
NEXUSHUB_DB_HOST_PORT=3307
NEXUSHUB_BACKEND_PORT=8080
NEXUSHUB_FRONTEND_PORT=5173
