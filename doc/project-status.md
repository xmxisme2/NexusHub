# 项目状态

## 已完成

- 对局创建新增可选“交换先后手”：服务端在本局快照中同时交换双方手牌和先手，允许机器人执行原 USER 必胜残局；原题目版本和证明结果保持不变。

- 默认入口已改为游客会话：游客自动获得临时 GUEST 身份，可浏览已发布残局和创建/进行斗地主对局；新建、编辑、保存、发布、删除、恢复、分析等操作仍需登录权限。

- 题库和详情已接通真实证明摘要，显示用户/机器人必胜；查询核对展示版本、完整状态及规则/求解器版本，无证明保持 UNKNOWN/null。后端 21 项测试通过，前端类型检查与构建通过。

- 残局库已改为整张卡片链接进入详情，移除“查看局面”按钮，支持键盘聚焦和打开。

- 门户系统入口已改为整张卡片可点击，移除“进入实验室”和“打开 OpsDesk”按钮；OpsDesk 默认地址为 `https://www.xmxisme.com/opsdesk/`，继续以新标签页打开。

- 门户 Hero 信息会随鼠标悬停或键盘聚焦的系统卡片切换，分别展示斗地主实验室和 OpsDesk 的系统说明与指标。

- 残局开始对局前新增“查看已证明策略”：进入独立可滚动推演页，游客可读取已发布残局的严格 PROVEN 结果、胜方和最多 5 条已验证代表路线；牌面按牌点数量转换为中文牌面描述，UNKNOWN 不显示必胜出牌。

- 残局详情已增加“← 返回残局库”入口和管理员“发布残局”操作，接入现有发布接口、提交中保护、成功/失败回显及普通用户权限说明；新增交互尚未同步 Figma。

- 建立 `AGENTS.md`，提取并调整 `base.md` 的公共工程、API、数据库、前端、安全和验证约定。
- 建立安全基线：密码使用 BCrypt 成本 12 单向散列，凭据版本可撤销会话，业务 POST 使用 CSRF 双提交，生产 Cookie Secure 与 Nginx 安全响应头可配置；生产仍需 Secret、HTTPS、共享会话和登录限流。
- 完成 V1 需求索引、范围、横切权限/状态/页面、identity/hub/puzzle/rules/game/analysis 模块需求。
- 完成 CLASSIC_V1 规则、局面、证明语义和求解边界。
- 生成 34 条 OpenAPI 设计接口、78 个 Schema、13 张表的 SQL 与字段字典。
- 创建 NexusHub Figma 文件并完成门户、残局库、残局编辑器、明牌对局台、分析中心、管理员入口、Cover、Foundations、Login、状态组件页及设计令牌；移动端明确不在本轮范围。

## 当前工作

- 方案与契约评审；已确认两名独立参与者、完全明牌、先出完获胜、用户/机器人先后手可配置、保留门户首页、四带二固定不可出且其他牌型按标准规则开放。已创建 Java 21 Spring Boot 后端骨架、Vue 3/Vite 前端门户骨架，并通过独立的 `nexushub-mysql`（MySQL 8.4）容器完成 NexusHub 本地库初始化。
- 题库第一步闭环已落地：后端提供搜索、详情、状态校验、保存草稿、发布接口的 Controller/Service/Mapper 分层；题库已改为 MySQL 持久化，测试库中有 L001～L004 四个可复核样例；前端增加桌面题库列表、空态和 API 模块。
- 后端已完成 Cookie 会话、CSRF 双提交校验、登录/退出/当前用户、管理员账号管理和凭据版本失效；题库保存、查询、发布已接入会话用户与对象权限。前端已完成门户、登录、题库列表、详情校验页及加载/空态/失败重试。
- 最小对局闭环已完成：纯 Java 规则引擎、对局创建/详情、出牌/PASS、服务端判胜、乐观并发版本和行动事件持久化；前端已接入明牌牌桌、点击牌面选牌、前后端双重校验、重开、弃局、返回题库和终局只读状态。后端 Wrapper 离线测试现为 19 tests、0 failures。
- 机器人回合 V1 已完成：单实例定时 Worker、任务领取和状态版本乐观锁、启发式合法出牌/PASS；前端在 BOT 回合自动轮询并刷新牌局。合法行动查询已补充 PASS。同步提示已接入对局页，按完整局面返回确定性合法建议，固定展示 UNKNOWN，不伪造必胜结论；重开按钮恢复创建快照并清理当前局行动；弃局会保留行动历史并以版本 CAS 结束对局。
- 对局复盘 V1 已完成：`POST /api/games/replay/detail` 按对局所有者读取初始快照和分页行动事件，桌面牌桌可进入复盘时间线；每个事件展示服务端保存的 afterState 摘要，不重新推断历史。
- 残局配置入库已完成：新增/编辑草稿页面可配置标题、描述、标签、难度、先手和双方牌量，前端校验后仍由服务端 `HandValidator` 校验并写入不可变版本；同时修正为两位玩家合计不得超过牌库同点数上限（大小王各1张）。
- 残局生命周期与去重已补齐：管理员可逻辑删除/恢复残局，删除只写 `deleted=1` 并保留版本、对局快照和证明记录；新增/编辑保存前后端均按完整初始局面检查重复，包含已删除记录，命中已删除残局时返回原题目标题、版本和复用引用，不重复创建证明任务。
- 题库范围已补齐：残局库默认展示当前用户的 `MINE` 范围，因此新建草稿可立即看到；可切换已发布视图，管理员额外拥有管理视图。`OPTIMAL` 对局现已接入逐回合实时 Minimax，TEST 局面已回归验证为 BOT 必胜。
- 证明任务 V1 已接入：启动时为已发布版本自动补建 `MINIMAX_V1` 任务，固定最多 5 个 Worker 并发；本地 L001～L005 已完成任务，其中结果均为 `PROVEN`，胜方分别按局面写入，策略材料暂标 `PARTIAL`。
- 已统一 Docker 运行配置：根目录 Compose 编排 `nexushub-mysql`、`nexushub-backend`、`nexushub-frontend`，后端使用 `docker` profile 通过服务名连接 MySQL，前端 Nginx 反向代理 `/api`。镜像构建和一次完整的登录、题库查询、建局链路已验证通过。
- 已接入生产 Jenkins 发布：保留根目录 `Jenkinsfile` 参考并配置 `nexushub-deploy` 任务，使用受限 SSH 调用远程构建脚本，在 Docker 中执行 Java 21/Node 20 构建、测试、健康检查和失败回滚；任务不依赖 Jenkins Git 插件拉取，避免 GitHub TLS 短暂中断阻塞发布，也不触碰现有 OpsDesk 服务。

## 已知问题

- OpsDesk 已配置为 `https://www.xmxisme.com/opsdesk/` 外链并从门户新标签页打开；身份架构、SSO、健康探测和代理能力仍未接入。当前环境无法从验证工具建立该公网连接，暂不把外部可达性作为本地构建通过条件。
- Figma 斗地主桌面稿已更新为 USER/BOT 两人牌桌、双方胜负文案、PASS 和四带二固定不可出的规则说明；当前组件仍以实现样板为主，尚未建立完整 Component Set 变体。
- 已生成本地测试库配置和 L001～L004 种子；已在独立 Docker 容器 `nexushub-mysql` 中执行 schema 与 seed，验证 13 张表、测试管理员账号、发布版本和复合版本指针。
- 已加入 `.github/workflows/ci.yml`，包含 OpenAPI 结构与规则校验、Markdown 链接、SQL 静态校验和 MySQL 8.4 空库 schema/seed 集成校验，并执行 Java Maven 测试、Vue 类型检查和构建。当前本地已通过契约生成、路径/schema 数量、规则禁用项、SQL 产物和 Docker MySQL 空库验证。初版不展示求解性能指标，只保留基础超时、取消和资源保护。
- 已参考 OpsDesk 引入 Maven Wrapper（`backend/mvnw.cmd`、`backend/.mvn`），对齐本机缓存的 Spring Boot 3.5.14/MyBatis 3.0.5；使用 JDK 21 执行 `backend\\mvnw.cmd -o clean test` 成功，当前为 19 tests、0 failures。前端已生成 `package-lock.json`，`npm run typecheck` 与 `npm run build` 均成功。
- 当前会话存储为单实例内存会话，生产部署前需替换为 Redis/共享会话存储；提示 V1 已实现为同步规则合法建议，证明任务已可执行，`OPTIMAL` 对局现已在机器人每回合直接执行带预算 Minimax，策略 DAG 持久化和完整幂等记录仍待实现。Windows 直接运行 Spring Boot 时仍有 Tomcat NIO loopback pipe 问题，Docker 是当前约定的运行方式。
- 本次复核先发现 Docker Desktop Linux 引擎未运行，启动 Docker Desktop 后已恢复；当前三个容器均为运行状态，MySQL healthy，后端 `/api/health` 返回 200，前端端口 5173 返回 200。该环境阻塞已解除。

## 下一步

- 继续维护 Docker 中 `nexushub-mysql` 的测试库；后续迁移必须新增脚本并在空库/升级库各验证一次。
- 继续接入 Redis 会话、提示/分析任务、幂等记录和审计日志；补齐 OPTIMAL 的持久化策略 DAG 和求解结果复用。
- 补齐 Figma 编辑器、移动端和管理状态，并导出实现所需节点/组件信息。
- 继续维护契约校验与 MySQL 空库回归；随后补齐策略 DAG 持久化、持久化幂等记录和 OPTIMAL 多分支对局绑定的 P2/P3 实现。



